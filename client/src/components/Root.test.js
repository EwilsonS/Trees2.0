import Root from './Root';
import { isArray } from 'util';

describe('#changeNodesToNumbers', () => {
  it('should genarate a random numbers array of given length, min and max', () => {
    let nodes = 5;
    let max = 10;
    let min = 0;

    // let cntn = Root
    expect(isArray(cntn)).toBe(true)
    console.log(cntn(nodes, max, min));
  })
});